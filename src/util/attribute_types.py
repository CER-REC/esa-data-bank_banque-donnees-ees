from enum import Enum


class AttributeType(Enum):
    """ Enum for python attribute types """
    # integers, float, string, and boolean.
    INTEGER = "INTEGER"
    FLOAT = "FLOAT"
    STRING = "STRING"
    BOOLEAN = "BOOLEAN"
